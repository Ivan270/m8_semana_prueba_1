
<section class="container">
    <h1 class="text-center display-3">Bienvenido {{usuario.nombre}}</h1>

    {{#if usuario}}
        <h2 class="display-6 fs-3">Nombre: {{usuario.nombre}}</h2>
        <h2 class="display-6 fs-3">RUT: {{usuario.rut}}</h2>
        <h2 class="display-6 fs-3">Email: {{usuario.email}}</h2>
    {{else}}
        <h2>No se ha podido mostrar la información de su perfil</h2>

    {{/if}}
</section>